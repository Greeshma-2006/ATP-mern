userapi route //activate user(change status to true)
UserApp.patch("/users/:id", async(req, res)=> {
    //get user if from url
      let uid = req.params.id;
    //check const [state, dispatch] = useReducer(//send user , second, third)
      
      
    )
})